const assert = require("chai").assert;
const istanbul = require("istanbul");

describe('MainTest', function() {
  describe('editSettings', function() {
    it('sets defaults for all possible fields', function() {
      
    });
  }); 
});

